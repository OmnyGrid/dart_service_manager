/// The canonical `dart_service_manager` package version.
///
/// Kept in sync with the `version` field in `pubspec.yaml`; a unit test asserts
/// the two never drift apart.
const String dartServiceManagerVersion = '1.2.1';
